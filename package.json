{
  "name": "shipwell-ext-webhook-tester",
  "version": "1.0.0",
  "description": "Test incoming webhooks by sending custom payloads and inspecting responses in real time.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^6.2.0",
    "vue": "^3.5.0"
  }
}
